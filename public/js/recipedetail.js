const connection = require('connection.js');

fetch('/api/recipes/' + recipeid)
  .then(response => response.json())
  .then(data => {
    
    const email = document.querySelector('#ingredients').value.trim();
    if (err) throw err;

  });
