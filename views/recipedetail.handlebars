{{!-- <div class="recipe-detail">
  <h2>{{title}}</h2>
   <div class="author">
    <h2>Author:{{userID}}</h2>
  </div>
  <div class="ingredients">
    <h2>Ingredients:</h2>
    <ul>
      {{#each ingredients}}
        <li>{{this}}</li>
      {{/each}}
    </ul>
  </div>
  <div class="instructions">
    <h2>Instructions:</h2>
    <ol>
      {{#each instructions}}
        <li>{{this}}</li>
      {{/each}}
    </ol>
  </div>
</div> --}}

<h1>{{title}}</h1>
<h2>{{author}}</h2>
<ul>
<h3>{{ingredients}}</h3>
<h3>{{instructions}}</h3>
</ul>